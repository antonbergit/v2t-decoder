# 🧪 Testing Report

**Дата**: 28.03.2026  
**Этап**: Phase 2 закрыт / Phase 3 начат  
**Статус**: In Progress

---

## 1) Проверка запуска и артефактов

### Test A: DEMO 60s на MKV (основной config)

- Config: [config.yaml](../config.yaml)
- Input: `test-example/2025-12-20_17-59-46.mkv`
- Model: `medium`
- Duration: 60 sec

Результат:
- ✅ Успешный запуск
- ✅ Создан [output/dialog_timeline.txt](../output/dialog_timeline.txt)
- ✅ Создан [output/dialog_timeline.srt](../output/dialog_timeline.srt)
- ✅ Создан [output/processing.log.json](../output/processing.log.json)

Ключевые метрики (из лога):
- Total runtime: ~86.9 sec
- `transcribe_s1`: ~57.5 sec, segments=9
- `transcribe_s2`: ~24.6 sec, segments=1
- Warning: `S2` дал мало сегментов, что само по себе не считается ошибкой для overlapping dialog

---

### Test B: DEMO 60s на MP4 (эталонный фрагмент)

- Config: временный `/tmp/v2t_phase2_mp4.yaml`
- Input: `test-example/2025-12-20_17-59-46 - 1of5.mp4`
- Model: `medium`
- Duration: 60 sec

Результат:
- ✅ Успешный запуск
- ✅ Артефакты сформированы в `output_phase2_mp4/`
- ✅ Язык определён как `ru`
- ⚠️ Сохраняется дисбаланс `S2` сегментов

---

### Test C: Tuning (language=auto, vad_filter=false)

- Config: временный `/tmp/v2t_phase2_tune.yaml`
- Input: `test-example/2025-12-20_17-59-46 - 1of5.mp4`

Результат:
- ✅ Выполнение успешно
- ❌ Качество хуже (частичная англоязычная транскрипция)
- ❌ Runtime заметно вырос (~538 sec)

Вывод:
- Для DEMO фиксируем `language: ru` и `vad_filter: true` как более стабильный профиль.

---

## 2) Проверка кодовой базы

- ✅ `python3 -m py_compile main.py src/*.py` — ошибок нет
- ✅ Логирование JSON формируется корректно
- ✅ Поддержка входа `.mkv` и `.mp4` работает

---

## 3) Итог на текущий момент

Phase 2 выполнен по плану:
- `DEMO` pipeline рабочий;
- артефакты создаются стабильно;
- реализован фиксированный default stream/mapping с опциональным prescan.

Автоподбор больше не является default-режимом.

---

## 4) Следующие тесты (Phase 2 continuation)

1. Стратегия по умолчанию: фиксированный stream/channel (успешный кейс).
2. Опциональный режим: `enable_channel_prescan=true` для быстрого прескана.
3. Сравнение с эталонным `test-example/*_dialog.txt` на первых 60 сек.

---

## 5) Результат после смены стратегии

Принятое правило:
- default: фиксированный профиль (`stream_index=1`, `S1=FL`, `S2=FR`)
- optional: быстрый прескан каналов по флагу

Проверка default (без прескана):
- ✅ pipeline выполняется успешно
- ✅ runtime улучшен относительно тяжелого авто-подбора

Проверка с пресканом (короткий DEMO 20 сек):
- ✅ стадия `prescan` отражена в логе
- ✅ выбранный mapping зафиксирован в `processing.log.json`

---

## 6) Phase 3 smoke test: production chunking

### Test D: PRODUCTION 70s, 2 chunks

- Config: [config.production-smoke.yaml](../config.production-smoke.yaml)
- Input: `test-example/2025-12-20_17-59-46.mkv`
- Model: `tiny`
- Device: `cpu`
- Mode: `production`
- `fragment_size_min=1`
- `max_video_duration_sec=70`

Результат:
- ✅ Успешный запуск
- ✅ Создан [output_phase3_smoke/dialog_timeline.txt](../output_phase3_smoke/dialog_timeline.txt)
- ✅ Создан [output_phase3_smoke/dialog_timeline.srt](../output_phase3_smoke/dialog_timeline.srt)
- ✅ Создан [output_phase3_smoke/processing.log.json](../output_phase3_smoke/processing.log.json)
- ✅ В логе зафиксирован `chunk_plan`
- ✅ Подтверждено разбиение на 2 чанка: `0–60s` и `60–70s`
- ✅ Подтверждены стадии `chunk_001` и `chunk_002`

Ключевые метрики:
- Total runtime: ~8.8 sec
- `chunk_count`: 2
- Финальный merge: `lines=11`

Вывод:
- базовый production chunking работает корректно;
- глобальные смещения таймлайна применяются;
- JSON-лог отражает план чанков и результат каждого чанка.

---

## 7) Phase 3 extended test: long chunk sequence

### Test E: PRODUCTION 190s, 4 chunks

- Config: [config.production-3chunk.yaml](../config.production-3chunk.yaml)
- Input: `test-example/2025-12-20_17-59-46.mkv`
- Model: `tiny`
- Device: `cpu`
- Mode: `production`
- `fragment_size_min=1`
- `max_video_duration_sec=190`

Результат:
- ✅ Успешный запуск
- ✅ Создан [output_phase3_3chunk/dialog_timeline.txt](../output_phase3_3chunk/dialog_timeline.txt)
- ✅ Создан [output_phase3_3chunk/dialog_timeline.srt](../output_phase3_3chunk/dialog_timeline.srt)
- ✅ Создан [output_phase3_3chunk/processing.log.json](../output_phase3_3chunk/processing.log.json)
- ✅ В `chunk_plan` зафиксировано 4 чанка: `0–60`, `60–120`, `120–180`, `180–190`
- ✅ Финальный merge: `43 lines`

Проверка таймлайна:
- ✅ Монотонность времени подтверждена (регрессии `timestamp reset` не обнаружено)
- ✅ Последняя реплика в TXT: `~187.14s`, что соответствует ожидаемому диапазону

Ключевые метрики:
- Total runtime: ~31.0 sec
- `chunk_count`: 4

Вывод:
- chunk-merge стабилен на последовательности из нескольких чанков;
- глобальные offsets применяются корректно;
- базовая реализация Phase 3 подтверждена расширенным smoke/validation тестом.

---

## 8) Проверка Docker в целевом окружении (decision test)

Проверено в текущем целевом окружении:

- `docker` установлен и доступен
- изначально не было доступа к `docker.sock` (права пользователя)
- после добавления пользователя в группу `docker` доступ к daemon появился
- `docker compose build` падает на `permission denied` в BuildKit mount
- `docker build` (legacy) падает на `AppArmor ... Permission denied`

Вывод:
- в текущем окружении релиз через Docker непригоден;
- выбран путь **native deployment**;
- Docker-попытка сохранена в отдельной ветке `phase4-docker-poc`.

---

## 9) Native deployment validation

Проверено по новой схеме запуска:

- создан `.venv`, зависимости установлены из [requirements.txt](../requirements.txt)
- helper script [scripts/run_v2t.sh](../scripts/run_v2t.sh) сделан executable
- запуск `./scripts/run_v2t.sh config.production-smoke.yaml` — ✅ completed

Вывод:
- путь native deployment в целевом окружении рабочий и подтверждён запуском pipeline.

